{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx turbo dev --parallel",
    "build": "npx turbo build"
  },
  "devDependencies": {
    "turbo": "^1.6.1"
  }
}
